[pytest]
markers =
    integration: tests that require network access or external services
    offline: tests that only use local files and deterministic computations
    randomized: tests that use deterministic seeded random sampling
